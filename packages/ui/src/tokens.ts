// Brand tokens (Phase 7). Storefront and admin both consume these.
// Real values land alongside shadcn theme generation.

export const brand = {
  name: "Ecom",
  radius: { sm: "0.375rem", md: "0.5rem", lg: "0.75rem", xl: "1rem" },
} as const;
